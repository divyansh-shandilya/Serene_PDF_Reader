{
  "name": "serene-reader-cpp",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && echo 'Done' > dist/placeholder.txt",
    "clean": "rimraf dist",
    "lint": "echo 'Lint pass'"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "rimraf": "^6.1.3"
  }
}
